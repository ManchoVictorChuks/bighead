/*
The copyWithin() method copies part of an array to another location in the
same array and returns it, without modifying its size.
*/

var num = [1, 2, 3, 4, 5, 6]
console.log(num.copyWithin(0 , 2))

